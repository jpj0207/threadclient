import React from 'react';
import BoardList from './BoardList';
import Header from './Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <BoardList></BoardList>
        </div>
    );
};

export default Home;